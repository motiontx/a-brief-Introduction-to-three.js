import { experience } from "../../../experiences/8_Textures/script";

if (typeof document !== "undefined") {
  experience();
}

const Experience = () => <canvas className="webgl" />;

export default Experience;
